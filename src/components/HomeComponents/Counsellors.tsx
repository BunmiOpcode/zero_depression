import React from "react";
import { Box, useBreakpointValue } from "@chakra-ui/react";

const Counsellors = () => {
    const slide = useBreakpointValue({
        base: 1,
        md: 1,
        lg: 2,
        xl: 3,
    });

  return (
    <Box>

    </Box>
  )
}

export default Counsellors